{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "nodemailer": "^6.9.8",
    "nodemon": "^3.0.2",
    "razorpay": "^2.9.2"
  },
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "nodemon server.js",
    "server": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --legacy-peer-deps && npm install --legacy-peer-deps --prefix frntend && npm run build --prefix frntend"
  },
  "author": "Parveen pra",
  "license": "ISC",
  "description": ""
}
